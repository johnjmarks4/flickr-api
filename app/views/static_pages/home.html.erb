<%= form_tag url: "/static_pages/home", method: "get" do %>
  <%= label_tag "Search for:" %>
  <%= text_field_tag :id %>
  <%= submit_tag "Submit" %>
<% end %>

<% if !@pictures.empty? %>
  <% @pictures.each do |pic| %>
    <%= image_tag pic %>
  <% end %>
<% end %>